This project currently uses SBT 0.11.x, but it isn't included in the repository.

From inside SBT, run `eclipse` to generate the necessary eclipse project files.
Run `container:start` to start the webapp.